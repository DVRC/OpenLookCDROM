
LispView Interface for the XView Ttysw Module

Documentation: tty.doc

Tty interface:
   notifier.lisp
   tty-defs.lisp
   tty.lisp
   xview-tty.lisp

Loading the file build.lisp into a LispView image recompiles and
reloads the tty interface.

Demo: tty-demo.lisp
